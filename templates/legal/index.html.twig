{% extends 'layout.html.twig' %}

{% block title %}Hello {{ Titre }} !{% endblock %}

{% block main %}
   <div clas="container-fluid" style="padding: 21px 60px;">
    <div class="row">
    <div class="col-md-12">
     <h2 class="top_h3">{{ Titre }}</h2>
    <div>
    <p>{{ Description }}</p>

    {% if CollectionTexts is defined %}
        {% for Text in CollectionTexts %}
            <br>
            <p>{{Text}}</p>
        {% endfor %}
    {% endif %}
    
    </div>
    </div>
    </div>
   </div>
{% endblock %}
